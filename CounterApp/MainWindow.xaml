<Window x:Class="CounterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CounterApp"
        mc:Ignorable="d"
        Title="Counter" SizeToContent="WidthAndHeight">
    <StackPanel Margin="100" Width="100">
        <Button Name="btnIncrement" Click="btnIncrement_Click">▲</Button>
        <Border Height="80">
            <TextBlock Name="tbCounter"
                       FontSize="30"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{Binding Path=CounterTextForeground, Mode=OneWay}"
                       Text="{Binding Path=CounterText, Mode=OneWay}"/>
        </Border>
        <Button Name="btnDecrement" Click="btnDecrement_Click">▼</Button>
    </StackPanel>
</Window>
